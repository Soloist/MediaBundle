{% extends 'SoloistBlockBundle:AdminBlock:configure.html.twig' %}

{% block form %}

    <div class="thumbnail">
        <div class="thumbnail-subject">
            {% if false %}
                <img src="{{ block.path | apply_filter('soloist_admin_media') }}" />
            {% endif %}
        </div>
        <div class="caption">
            {{ form_widget(form.path_bg) }}
            <a href="{{ path('soloist_admin_media_index') }}?nolayout" class="btn soloist-media-add">Choisir une image</a>
        </div>
    </div>
    <div class="thumbnail">
        <div class="thumbnail-subject">
            {% if false %}
                <img src="{{ block.path | apply_filter('soloist_admin_media') }}" />
            {% endif %}
        </div>
        <div class="caption">
            {{ form_widget(form.path_fg) }}
            <a href="{{ path('soloist_admin_media_index') }}?nolayout" class="btn soloist-media-add">Choisir une image</a>
        </div>
    </div>

    {{ form_rest(form) }}

{% endblock %}
